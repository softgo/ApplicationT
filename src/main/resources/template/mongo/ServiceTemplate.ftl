package ${baseFilePackage}.service;

import ${baseFilePackage}.entity.${className};
import com.application.base.mongo.api.MongoBaseService;

/**
 * service 的接口定义(实际使用中可以自行添加)
 * @author bruce
 */
public interface ${className}Service extends MongoBaseService<${className}> {

}
